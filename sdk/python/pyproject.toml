[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "synapse_memory"
version = "0.1.0"
authors = [{name = "AI Studio Agent", email = "assistant@google.com"}]
description = "Production-grade semantic memory engine with semantic deduplication, decay-based pruning, and token-aware context packing."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
